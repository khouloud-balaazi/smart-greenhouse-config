spring:
  application:
    name: controle-service
  datasource:
    url: jdbc:mysql://localhost:3306/control_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    host: leopard.lmq.cloudamqp.com
    port: 5672
    username: qiyhxqyw
    password: cqmNazI1t4eaWlutnCjEwBYqat_BNcbX
    virtual-host: qiyhxqyw
    ssl:
      enabled: false

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    db:
      enabled: true
    rabbit:
      enabled: true

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
