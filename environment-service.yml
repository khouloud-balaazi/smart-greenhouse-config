spring:
  application:
    name: environment-service

  datasource:
    url: jdbc:mysql://environment-db:3306/environment_db?useSSL=false&serverTimezone=UTC
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  rabbitmq:
    host: leopard.lmq.cloudamqp.com
    port: 5672
    username: qiyhxqyw
    password: cqmNazI1t4eaWlutnCjEwBYqat_BNcbX
    virtual-host: qiyhxqyw

server:
  port: 8089

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
